name: GitHub → Discord

on:
  push:
    branches: [ main ]
  pull_request:
    types: [opened]

jobs:
  discord:
    runs-on: ubuntu-latest
    steps:
      - name: Send message to Discord
        run: |
          curl -H "Content-Type: application/json" \
          -X POST \
          -d "{\"content\": \"📌 Repo: $GITHUB_REPOSITORY\n🔧 Event: $GITHUB_EVENT_NAME\"}" \
          ${{ secrets.DISCORD_WEBHOOK }}
